{
  "name": "cardwars-showdown",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "bun test",
    "cli": "bun sim/cli.ts",
    "runner": "bun sim/runner.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "bun-types": "^1.3.14",
    "typescript": "^7.0.2"
  }
}
